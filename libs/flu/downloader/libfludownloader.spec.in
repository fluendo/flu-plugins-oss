Name:		libfludownloader
Version:	@VERSION@
Release:	2.flu
Summary:	Fluendo HTTP downloader library

Group:		System/Libraries
License:	Commercial
URL:		http://www.fluendo.com/
Source:		libfludownloader-%{version}.tar.bz2
BuildRoot:      %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)

BuildRequires:  pkgconfig curl-devel glib2-devel

%description
Fluendo HTTP downloader library which supports HTTP pipelining using CURL.

%package devel
Summary:        Fluendo HTTP downloader library headers
Group:		Development/Libraries
Requires:       libfludownloader = %{version}-%{release}

%description devel
This package contains all of the headers and dynamic libraries for libfludownloader. You will only need this package if you are doing development.

%package static
Summary:        Fluendo HTTP downloader library static libs
Group:		Development/Libraries
Requires:       libfludownloader = %{version}-%{release}

%description static
This package contains all of the headers and static libraries for libfludownloader. You will only need this package if you are doing development.

%prep
%setup -q -n libfludownloader-%{version}

%build
%configure
make CFLAGS="-fno-strict-aliasing"

%install
rm -rf $RPM_BUILD_ROOT
%makeinstall

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root)
%doc README NEWS
%{_libdir}/*.so.*

%files devel
%defattr(-,root,root)
%{_includedir}/*
%{_libdir}/*.so
%{_libdir}/*.la
%{_libdir}/pkgconfig/*.pc

%files static
%defattr(-,root,root)
%{_includedir}/*
%{_libdir}/*.a
%{_libdir}/*.la
%{_libdir}/pkgconfig/*.pc

%changelog
* Thu Oct 24 2013 Julien Moutte <julien@fluendo.com>
- Initial spec file for libfludownloader.
